/**
 * commands.ts
 * This file exists so Outlook can load a "function file" if needed.
 * This MVP uses ShowTaskpane actions, so there are no execute-function commands yet.
 */
Office.onReady(() => {
  // No-op.
});
